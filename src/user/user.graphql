type User {
  id: ID!
  username: String!
  created: String!
  bookmarks: [Idea!]
  ideas: [Idea!]
  comments: [Comment!]
}

type Query {
  users(page: Int): [User!]
}

# export class UserRO {
#   id: string;
#   username: string;
#   created: Date;
#   token?: string;
#   bookmarks?: IdeaEntity[];
# }